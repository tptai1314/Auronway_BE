const { prisma } = require('../../shared/db'); // adjust path to your prisma singleton

async function example() {
  // Placeholder query
  return { msg: 'registrations repo ok' };
}

module.exports = { example };
